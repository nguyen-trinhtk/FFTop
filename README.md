# FFT-op: An FFT library for CPU and GPU

This repo provides an optimized implementation depending on the underling hardware.

### Run

### Test

### Bench

### Supported hardware configs
- Auto detect: now supports
+ CPU: ...
+ (SIMD only on AVX/AVX2/NEON)
+ Optional OpenMP
+ GPU: NVIDIA GPUs only for now (CUDA-supported)